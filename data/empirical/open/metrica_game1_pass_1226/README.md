# Metrica Sample Game 1 empirical excerpt

This directory contains a source-pinned 0.56-second excerpt around frame 1226 and the synchronized pass from anonymous `Player10` to `Player8`.

It is real provider tracking and event data, not a synthetic scenario. The original 0–1 coordinates are preserved, with a normalized long-form mirror for the empirical renderer. The players are anonymized, so this excerpt supports geometry and temporal validation, not named-player claims. It contains no literal gaze, body pose, or force measurement.
