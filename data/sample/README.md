# Sample data

Run:

```bash
midfielders-eye demo
```

The command creates the complete reproducible synthetic bootstrap dataset in `artifacts/demo/`.
Generated data is not committed here to keep the repository lightweight and to make provenance explicit.
