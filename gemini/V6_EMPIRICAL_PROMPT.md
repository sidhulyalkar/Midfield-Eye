# Gemini v0.6 Empirical Implementation Pass

Use this after the base frontend is functional.

Implement `/empirical`, `/empirical/sources`, `/empirical/experiments/:experimentId`, `/evidence-ledger`, and `/capture-studio` from the static/API contracts. Treat `artifacts/showcase/empirical` as the source of truth. Add no invented data. Every chart tooltip must identify source, tier, and whether the displayed value is measured or inferred.
